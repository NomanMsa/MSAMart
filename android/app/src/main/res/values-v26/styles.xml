<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/app_bg</item>
        <item name="android:textColor">#000000</item>
		<item name="android:datePickerDialogTheme">@style/MyDialogTheme</item>
		<item name="android:timePickerDialogTheme">@style/MyDialogTheme</item>
        
         <item name="android:statusBarColor">@color/splashcolor</item>
         
         
         <!-- changes for white screen -->
         <!-- Changes to fix oreo screen orientation fix-->
             <item name="android:windowIsTranslucent">false</item>
    </style>

</resources>
